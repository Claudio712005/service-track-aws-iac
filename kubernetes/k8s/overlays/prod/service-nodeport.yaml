apiVersion: v1
kind: Service
metadata:
  name: service-track-app-nodeport
  labels:
    app: service-track-app
spec:
  type: NodePort
  selector:
    app: service-track-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
