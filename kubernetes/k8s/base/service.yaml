apiVersion: v1
kind: Service
metadata:
  name: service-track-app
  labels:
    app: service-track-app
spec:
  type: ClusterIP
  selector:
    app: service-track-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
